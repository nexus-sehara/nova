import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function checkSessions() { try { const sessionCount = await prisma.session.count(); console.log('Total sessions in database:', sessionCount); const sessions = await prisma.session.findMany({ take: 3 }); console.log('Sample sessions:', JSON.stringify(sessions, null, 2)); } catch (error) { console.error('Error accessing database:', error); } finally { await prisma.$disconnect(); } } checkSessions();
